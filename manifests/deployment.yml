apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  strategy:
    rollingUpdate:
      maxSurge: 3
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: outoftheparkacr.azurecr.io/user-service
          imagePullPolicy: Always
          ports:
            - containerPort: 443
              name: user-http
---
apiVersion: v1
kind: Service
metadata:
  name: user-service-deployment
spec:
  ports:
    - name: user-service
      protocol: TCP
      port: 443
      targetPort: 443
  selector:
    app: user-service
  type: ClusterIP